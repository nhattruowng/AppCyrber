## Get started

1. Install dependencies

   ```bash
   npx expo prebuild --clean
   ```

2. Start the app

   ```bash
    npx expo run:android
   ```

4. ```bash
    npx eas-cli build:configure
    npx eas-cli build

   ```

